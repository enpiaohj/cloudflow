<UserControl x:Class="CloudFlow.App.Views.JobsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml">

    <Grid Margin="28,22,28,24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <TextBlock Text="任务" Style="{StaticResource Cf.PageTitle}" />
            <TextBlock Text="平台所有写操作的统一操作总线 —— 每条操作均经验证与审计。"
                       Style="{StaticResource Cf.Subtitle}" Margin="0,4,0,0" />
        </StackPanel>

        <Grid Grid.Row="1" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ComboBox Grid.Column="0" Width="180" Height="34"
                      ItemsSource="{Binding StatusOptions}"
                      SelectedItem="{Binding StatusFilter}" />
            <Button Grid.Column="2" Style="{StaticResource Cf.SecondaryButton}" Height="34"
                    Command="{Binding RefreshCommand}">
                <StackPanel Orientation="Horizontal">
                    <ui:SymbolIcon Symbol="ArrowClockwise24" FontSize="14" />
                    <TextBlock Text="刷新" Margin="7,0,0,0" />
                </StackPanel>
            </Button>
        </Grid>

        <Border Grid.Row="2" Style="{StaticResource Cf.Card}" Padding="6,4,6,6" Margin="0,14,0,0">
            <DataGrid ItemsSource="{Binding Jobs}" Style="{StaticResource Cf.DataGrid}">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="时间" Binding="{Binding CreatedAt, StringFormat=MM-dd HH:mm:ss}" Width="160" />
                    <DataGridTextColumn Header="账户" Binding="{Binding AccountDisplayName}" Width="120" />
                    <DataGridTextColumn Header="操作" Binding="{Binding Display}" Width="*" />
                    <DataGridTemplateColumn Header="状态" Width="130">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Border Style="{StaticResource Cf.Badge}">
                                    <TextBlock Text="{Binding Status, Converter={StaticResource StatusText}}" Style="{StaticResource Cf.BadgeText}" />
                                </Border>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    <DataGridTextColumn Header="风险" Binding="{Binding Risk, Converter={StaticResource StatusText}}" Width="80" />
                    <DataGridTextColumn Header="结果" Binding="{Binding Summary}" Width="*" />
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>
