# Changelog

本项目遵循 [Semantic Versioning](https://semver.org/lang/zh-CN/)。
每个正式版本的**完整**变更记录见 `releases/vX.Y.Z/CHANGELOG.md`（含验证结果与已知问题）。

---

## v0.6.1 — 2026-09-14

顶栏账户名改为显示登录名；补充功能文档。

**主要修复**
- 顶栏账户区域原来显示账户展示名，两个不同账户展示名完全相同时（比如同一个人的工作账户和
  个人账户）分不清当前生效的是哪一个；改为显示登录名（UPN），在同一 Provider 下能唯一区分。
  展示名仍保留在悬浮提示与账户切换菜单的副标题里。

**文档**
- README 新增「功能」一节，完整介绍多账号/多租户/多订阅、虚拟机全生命周期运维、资源清理
  （含本轮新增的下拉筛选器）、Operation Engine、任务中心、SSH 终端、成本洞察
- 用户手册补充资源清理筛选器（类型/资源组/区域/订阅下拉）、删除确认高亮、清晰错误信息、
  顶栏账户登录名显示的说明

**验证**：`dotnet build CloudFlow.sln -c Release -t:Rebuild` 0 警告 0 错误；513 项测试全部通过；单文件 EXE 启动验证通过。

**主要已知问题**
- 无

完整清单见 [`releases/v0.6.1/CHANGELOG.md`](releases/v0.6.1/CHANGELOG.md)。

---

## v0.6.0 — 2026-09-14

删除操作稳定性修复合集，并新增资源组/所有资源筛选器。

> 本版本合并了 v0.5.2~v0.5.9 这 8 个未推送到 GitHub 的本地迭代版本——它们是同一次测试会话
> 里连续发现问题、连续修复产生的中间版本，从未发布也从未被外部使用，因此不再作为独立版本
> 保留，合并为这一个正式版本发布。

**主要修复（删除操作稳定性）**
- "所有资源"/"资源组"页删除资源/资源组时的 `ItemContainerGenerator` 崩溃（"某个 ItemsControl
  与它的项源不一致"）：批量删除与单项删除各自独立复现过一次，根因最终定位到搜索过滤器在
  列表自身 `CollectionChanged` 事件分发过程中被同步刷新，与 DataGrid 正在处理的变更冲突；
  两个页面统一改为延后到事件分发结束后再刷新过滤器
- 应用崩溃后卡在"执行中"的任务，下次启动时会被识别为失败，不再永久显示"进行中"
- 删除资源/资源组失败时不再展示完整 HTTP 诊断转储，改为摘出 Azure 错误信封里的一句话
  （通常会点名是被谁引用/占用导致删不掉）；覆盖 `DeleteAsync` 与执行前存在性预检查
  `ExistsAsync` 两条路径（后者是本轮新增的存在性预检查引入的遗漏点）
- 两个删除 Handler 补上执行前的存在性预检查，避免对已被别处删掉的目标重复撞 404
- "资源组"/"所有资源"页点击刷新按钮时绕过 10 分钟内存缓存，不再显示已删除的陈旧数据

**主要修复（创建虚拟机向导）**
- 可编辑下拉框（订阅/资源组/区域/规格/镜像）在用户打字覆盖预选项后仍会误用旧的
  `SelectedItem`（真实复现：资源组框输入新名称 `RG-LT1-SG`，最终却用了已有的 `RG-AC-KR`）

**主要能力**
- 删除确认对话框中的"目标资源"改用危险色高亮显示
- 创建虚拟机向导的资源组下拉框支持边打字边筛选已有资源组
- "所有资源"页新增类型/资源组/区域三个下拉筛选器；"资源组"页新增区域/订阅两个下拉筛选器；
  均保留原有名称搜索框。每个下拉旁边有固定的说明性文字（不随选中值变化），下拉默认值也
  带字段名（如"全部类型"）

**验证**：`dotnet build CloudFlow.sln -c Release -t:Rebuild` 0 警告 0 错误；512 项测试全部通过；单文件 EXE 启动验证通过。

**主要已知问题**
- 本版本新增/修改的所有 UI（筛选器、创建向导下拉框、删除确认高亮）均未做交互式 UI 回归
  验证——测试过程中一次自动化点击误触了另一个正在运行实例上已打开的确认框，导致真实删除了
  Azure 账户中的 `NetworkWatcherRG`（空资源组，Azure 会自动重建），此后本轮会话对存活的
  真实账户停止了任何自动化点击，验证仅覆盖编译与单元/静态测试

完整清单见 [`releases/v0.6.0/CHANGELOG.md`](releases/v0.6.0/CHANGELOG.md)。

---

## v0.5.1 — 2026-09-14

修复批量删除资源组/资源时的崩溃，并改善批量删除确认框里目标资源的显示。

**主要修复**
- 批量删除 3 项以上时崩溃（`ItemsControl` 与项源不一致）：批量执行循环内不再逐项立即从列表
  移除，改为整批跑完后一次性替换列表
- 批量确认框改为显示「名称（区域代码 · 中文名）」，不再堆原始 Resource ID 或只显示翻译

**验证**：`dotnet build CloudFlow.sln -c Release -t:Rebuild` 0 警告 0 错误；489 项测试全部通过；单文件 EXE 无 `appsettings.json` 启动验证通过。

**主要已知问题**
- 修复后的批量删除未在真实 Azure 账户上重新触发端到端回归（原崩溃基于真实崩溃日志定位）

完整清单见 [`releases/v0.5.1/CHANGELOG.md`](releases/v0.5.1/CHANGELOG.md)。

---

## v0.5.0 — 2026-09-14

个人 Microsoft 账户登录支持首次使用时自动下载 Azure CLI Runtime；仓库采用 GPL-3.0 许可证；
补齐面向使用者与贡献者的公开文档；产品设计过程文档迁出仓库并清理相应 Git 历史。

**主要能力**
- 首次「添加个人 Microsoft 账户」且本机缺少 Runtime 时自动下载（官方 GitHub Release，校验
  SHA-256 通过才使用），此前所有版本这条登录路径实际上只在开发机上能用
- 新增 `docs/` 下六份公开文档（用户手册、认证说明、架构、开发指南、Provider 开发规范）与
  `CONTRIBUTING.md`

**主要变更**
- 仓库改用 GPL-3.0 许可证
- 产品设计过程文档迁出仓库、Git 历史相应清理（含此前一份历史提交中曾短暂出现的真实客户端 ID
  与内部验证记录），已重新发布 `v0.1.0`～`v0.4.1` 对应 Tag

**验证**：`dotnet build CloudFlow.sln -c Release -t:Rebuild` 0 警告 0 错误；488 项测试全部通过
（含一项真实下载 Azure CLI Runtime 的端到端集成测试）；单文件 EXE 无 `appsettings.json` 启动
验证通过。

**主要已知问题**
- 公开文档目前只有一张 Demo 模式截图，其余页面截图尚未提供

完整清单见 [`releases/v0.5.0/CHANGELOG.md`](releases/v0.5.0/CHANGELOG.md)。

---

## v0.4.1 — 2026-09-14

修复正式版单文件 EXE 无法配置 Azure 登录服务的问题。

**主要修复**
- 「设置 → 账户 → 登录服务」可直接填写应用（客户端）ID；账户范围改为选择（多个组织 = `organizations` / 单个组织 + 目录（租户）ID）
- 配置保存到 `%LOCALAPPDATA%\CloudFlow\appsettings.json`，优先于程序目录配置；首次配置立即生效
- 未配置提示改为指向设置页，不再要求复制模板文件

**验证**：`dotnet build CloudFlow.sln -c Release -t:Rebuild` 0 警告 0 错误；481 项测试全部通过；单文件 EXE 无 `appsettings.json` 启动并显示配置入口。

**主要已知问题**
- 在设置页保存配置后完成登录的端到端验证未执行

完整清单见 [`releases/v0.4.1/CHANGELOG.md`](releases/v0.4.1/CHANGELOG.md)。

---

## v0.4.0 — 2026-09-14

资源组与所有资源支持批量删除；首页随窗口自适应宽度与高度。

**主要能力**
- 「资源组」「所有资源」列表支持勾选与全选，「删除所选（N）」合并为一次确认（须输入「删除 N 个资源组 / 项资源」），逐项执行并显示进度；每项仍是独立 Job 与审计记录，取消时作废本轮待审批任务
- 所有资源按依赖顺序删除（网卡 → 其它 → 虚拟网络 → 网络安全组 / 路由表）
- 首页不再限宽 1200，最大化时铺满；「最近操作 / 注意项」随窗口高度伸缩，底部不再留空
- 首页读取状态增加旋转图标；Scope「全部可访问订阅」改为「所有订阅」

**主要修复**
- 删除失败时该行不再从列表消失（按 Job 状态判断，只有成功才移除）

**验证**：`dotnet build CloudFlow.sln -c Release -t:Rebuild` 0 警告 0 错误；460 项测试全部通过；单文件 EXE 无 `appsettings.json` 启动验证通过。

**主要已知问题**
- 批量删除与单项删除在真实 Azure 上的端到端验证未执行

完整清单见 [`releases/v0.4.0/CHANGELOG.md`](releases/v0.4.0/CHANGELOG.md)。

---

## v0.3.0 — 2026-09-14

新增「资源」管理入口，创建虚拟机支持按需新建网络并接入真实 Azure 目录，完成一轮界面专业化重设计。

**主要能力**
- 侧栏「资源」分组：「所有资源」「资源组」两个列表，支持搜索；删除资源组 / 单个资源经 Operation Engine，不可绕过确认，删除资源组须输入名称
- 创建虚拟机：资源组 / 虚拟网络 / 子网不存在则新建、存在则复用（设计文档 v3.2 §87.2）
- 创建向导接入真实区域 / 规格 / 资源组目录，提示镜像与规格世代不匹配，显示预估月费
- 任务子步骤进度上报与顶栏「任务进行中」徽标；创建 / 删除对话框就地显示进度与失败原因
- 界面专业化重设计与全站文案规范化；统一确认对话框替换原生 `MessageBox`

**主要修复**
- 删除虚拟机时单件连带资源失败不再中断后续清理；已停止的虚拟机可直接解除分配
- 修复 Wpf.Ui 3.0.5 高码位图标乱码，并新增守护测试

**验证**：`dotnet build CloudFlow.sln -c Release -t:Rebuild` 0 警告 0 错误；459 项测试全部通过；单文件 EXE 无 `appsettings.json` 启动验证通过。

**主要已知问题**
- 删除资源组 / 单个资源的真实 Azure 端到端验证未在发布验证中执行
- 同时创建多台虚拟机时进度提示可能串台

完整清单见 [`releases/v0.3.0/CHANGELOG.md`](releases/v0.3.0/CHANGELOG.md)。

---

## v0.2.0 — 2026-09-13

创建 / 删除虚拟机的 P1 界面与执行链路正式交付，并移除成本洞察的模拟账单金额。

**主要能力**
- 两步「创建虚拟机」向导：已有资源组 / 子网、Ubuntu 或 Windows 镜像、常用规格、可选 Standard 静态公网 IP、SSH 公钥或密码认证
- 创建动作统一经过 Operation Engine；密码先写入 DPAPI 凭据库，待审批请求只保存凭据 Id
- 真实 ARM 创建链路按公网 IP → NIC → VM 顺序执行，创建前拒绝同名 VM / NIC / 公网 IP 覆盖
- 列表页创建入口替换原占位提示；创建或删除完成后自动刷新清单
- 删除虚拟机 UI（行菜单 + 连带资源勾选确认）随本版本发布
- 未登录时成本洞察显示空态并提示登录，不再展示模拟金额或模拟环比

**验证**：`dotnet build CloudFlow.sln -c Release -t:Rebuild` 0 警告 0 错误；完整测试结果见版本快照。

**主要已知问题**
- 批量 SSH 连接、批量主机指纹确认与创建 VM 尚未完成真实 Azure 端到端 UI 验证
- 快照入口仍是占位弹窗，后端已实现

完整清单见 [`releases/v0.2.0/CHANGELOG.md`](releases/v0.2.0/CHANGELOG.md)。

---

## v0.1.0 — 2026-09-13

首个正式发布。P1 阶段（VM 运维 + 多订阅）功能面完整。

**主要能力**
- 多订阅清单与跨订阅 VM 列表（Azure Resource Graph）；MSAL 设备码 + 嵌入式 Azure CLI 两条身份
- VM 运维全部经 Operation Engine（启停 / 重启 / 关机 / 解除分配 / 改规格 / 快照 / 网络规则）
- 应用级底部终端面板与多标签 SSH 会话（WebView2 + xterm.js）
- 全局 SSH 凭据库（DPAPI，元数据与密文分文件）；连接入口覆盖详情页、列表菜单与批量连接
- 主机指纹两段式校验：首次记录需确认、变化强警告、绝不静默接受
- **创建 / 删除虚拟机纳入 P1 范围**（设计文档 v3.1 §87；引擎侧完成，界面待实现）

**验证**：`dotnet build -c Release -t:Rebuild` 0 警告 0 错误；421 项测试全部通过

**主要已知问题**
- 批量连接与创建/删除虚拟机的界面尚未端到端验证（创建/删除的 UI 亦未实现）
- 快照入口仍是占位弹窗，后端已就绪

完整清单见 [`releases/v0.1.0/CHANGELOG.md`](releases/v0.1.0/CHANGELOG.md)。

---

## 版本与快照约定

- 正式发布使用 `MAJOR.MINOR.PATCH`，Tag 为 `vX.Y.Z`（不加产品名前缀）。
- 每次正式发布创建 `releases/vX.Y.Z/`：`source/`（源码快照）与 `CHANGELOG.md` **入库**；
  **二进制产物不入库**，作为 GitHub Release 的附件交付。
- 已发布的快照不可覆盖：发现 Bug 时发新版本，而不是修改旧快照。
